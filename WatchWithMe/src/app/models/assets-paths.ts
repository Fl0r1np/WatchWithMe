import { environment } from "@environments/environment.development"

const assetsPath = `${environment.apiDomain}/assets`;

export const AssetsPaths = {

    // Path for avatars
    avatars: `${assetsPath}/avatars/`,

}